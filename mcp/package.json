{
  "name": "adlift-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AdLift ad-creative optimiser exposed as MCP tools over Streamable HTTP. Deployable on Alpic.",
  "scripts": {
    "dev": "node server.js",
    "build": "esbuild server.js --bundle --platform=node --format=esm --outfile=dist/index.js --packages=external",
    "start": "node server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^5.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "engines": {
    "node": ">=20"
  }
}
